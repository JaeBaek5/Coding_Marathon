# Claude Agent Instructions

Follow `AGENTS.md` first. This file exists so Claude-style agents discover the same repo workflow.

Required workflow summary:

- Read `.omo/plans/restaurant-recommendation-swarm-webapp.md` before implementation.
- Keep the plan current when scope, task order, verification, or API contracts change.
- Work on a unique branch, not `main`.
- Publish a draft PR at handoff, even for partial work, so other agents can see current state and avoid conflicts.
- Include plan task numbers, verification results, and `.omo/evidence/` artifacts in the PR body.
- Do not overwrite another agent's changes or tracked environment files.

For general coding behavior, also follow `docs/CLAUDE.md`.
